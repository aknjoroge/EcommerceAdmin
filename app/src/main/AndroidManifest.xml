<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.porkpitadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_adminpork"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_adminpork"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".paymentdetails"></activity>
        <activity android:name=".moredetails" />
        <activity android:name=".productdetails" />
        <activity android:name=".allusers" />
        <activity android:name=".orderpaid" />
        <activity android:name=".allproducts" />
        <activity android:name=".addproduct" />
        <activity android:name=".orders" />
        <activity android:name=".home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>